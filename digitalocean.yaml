name: sorturl-backend
region: blr
services:
- name: backend
  source_dir: Backend
  dockerfile_path: Dockerfile
  github:
    branch: master
    deploy_on_push: true
  http_port: 8080
  instance_count: 1
  instance_size_slug: basic-xxs
